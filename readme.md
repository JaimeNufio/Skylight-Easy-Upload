# Skylight Easy Upload
Tool for quickly uploading images to your Skylight Frame leveraging their email utlitiy.

# Usage
Make a `config.json` that looks like the `configSample.json`
In a folder called "Images" in the same directory as `run.py`, dump your images.
Run `run.py` and it'll begin uploading the images.

# TODO
Cleanup the script.